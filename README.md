# freecad-mcp-use-client

FreeCAD MCP Client using mcp-use.

## Usage
First, you need to install FreeCAD and [FreeCAD MCP addon](https://github.com/neka-nat/freecad-mcp).


```bash
uv run python freecad_mcp_use_client.py <image_path>
```
